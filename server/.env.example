# Variables de entorno para el servidor de GateG
# Copia este archivo a .env y ajusta los valores según tu configuración

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=gateg

# Puerto del servidor backend
PORT=4000

# Clave secreta para JWT (cambia por una clave segura en producción)
JWT_SECRET=gateg-super-secret-key-change-in-production

# Entorno (development, production)
NODE_ENV=development